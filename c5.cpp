#include <iostream>
#include <math.h>
using namespace std;
int main() 
{  
    
    float n,x,o;
    cout<<"Enter the number and the power to which it should be raised";
    cin>>x>>n;
    o=pow(x,n);
    cout<<"required answer is "<<o;
    return (0);
    
    
}
